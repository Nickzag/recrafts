# Single-Image Smoke

One sanitized empty-state screenshot verifies the single-image adapter. Its output must remain `source_coverage: limited` and `system_confidence: partial`; it cannot claim a complete visual system.
