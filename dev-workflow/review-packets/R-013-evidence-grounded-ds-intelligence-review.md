# Task R-013 — Evidence-grounded Design System Intelligence Review Packet

Requested verdict: PASS / FAIL for local R-013 capability and package qualification. A PASS may authorize the separately governed Linux Qualification; it does not merge or publish this candidate.

Review the following high-risk boundaries:

1. Ajv is the only structural authority; Runtime checks cross-object semantics only.
2. `verify-source-fidelity` and `validate-design` use the existing Operation surface and require Intelligence only under `r013-evidence-grounded`.
3. Gate A opens and hashes declared Source, Reconstruction, and Overlay files and binds Source IDs to Intelligence coverage.
4. Gate B binds maturity, components, product composition, Preview and downstream result while continuing to state `proves_source_fidelity: false`.
5. Candidate comparison emits all five semantic classes and an unauthorized Decision Ledger Candidate; it never votes, averages, selects a winner, or authorizes promotion.
6. The non-Craft holdout contains no Craft layout/value oracle; the frozen benchmark fixture is referenced by hashes and remains unmodified.
7. The attached tarball is the exact artifact used for Darwin clean installation and 11-operation installed smoke.

Known limits: no Linux execution, real Blind Agent, new Craft benchmark, Owner PASS, Accepted Release, merge, publish, or Layoutcrafts implementation is claimed.
