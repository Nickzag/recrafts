# Benchmark lessons

The authoritative lesson registry is `dev-workflow/research/r013/`. The frozen benchmark is read-only and used only to identify generic capabilities. Candidate identities are anonymized in the registry. All three runs reviewed 13/13 sources; their complementary strengths were detailed extraction, system maturity/downstream rules, and provenance/geometry/honest failure. Their historical Gate scores are not comparable and are never used for voting.
