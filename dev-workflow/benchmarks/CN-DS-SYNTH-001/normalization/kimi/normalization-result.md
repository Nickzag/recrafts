# KIMI Normalization Result

- Runtime: `da0ba9ee...`
- Runtime dependencies: installed from exact tarball package manifest in an isolated temporary directory
- Source mutated: no
- Parse: `failed` (`design.md must begin with YAML Front Matter`)
- Compile: `failed` for the same canonical-format reason
- Gate B: `NOT_REQUALIFIED`
- Layoutcrafts Utility: `UNQUALIFIED`

Historical Gate scores are retained only as input context and are not comparable.

The interop response intentionally reports only `INTERNAL_ERROR`; a read-only call to the parser from the same exact tarball established the diagnostic above. The frozen source was not changed.
