# CN-DS-SYNTH-001 Canonical Qualification

## Result

```text
parse                         PASS
compile                       PASS
Design IR                     PASS
SOURCE_FIDELITY               PASS
DESIGN_SYSTEM_COHERENCE       PASS
browser-bound visual proof    PASS
LAYOUTCRAFTS_UTILITY           DEGRADED
agent_usable                   false
Owner acceptance              PENDING
```

Gate A is the task-authorized shared Mainline rule-fidelity checklist against the exact 13-image Tier-A Source Pack. It does not claim pixel cloning and does not use `preview.html` as Source Fidelity evidence.

Gate B is the frozen `recrafts-0.5.0-rc.1` production coherence gate. It passed schema/IR, token bindings, state coverage, composition grammar, accessibility, unknown governance, deterministic Preview integrity and real three-viewport Playwright evidence.

## Bounded degradation

The Candidate is usable for a desktop-first Layoutcrafts application grammar. It is degraded for responsive implementation because no phone/tablet product screenshots or exact breakpoints exist. The frozen renderer is also a machine coverage proof rather than an Owner-quality coded product mockup. Neither limitation may be silently repaired by generic SaaS or component-library defaults.

This qualification does not create an Accepted Design Release, Owner PASS or `agent_usable=true` state.
