# Fidelity boundary

The packaged fidelity operation validates a supplied bounded fidelity report. This RC does not include embedded rendering, pixel-perfect reconstruction, or unrestricted website cloning.
